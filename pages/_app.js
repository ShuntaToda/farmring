// import AuthProvider from "../lib/AuthContext";
function MyApp({ Component, pageProps }) {
  return (
    <Component {...pageProps} />
    // <AuthProvider>
    // </AuthProvider>
  );
}

export default MyApp;
